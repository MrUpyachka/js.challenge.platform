package qq.upyachka.js.contest.platform.script.executor;

/**
 * TODO.<br>
 * Created on 11.03.17.
 * @author upyachka.
 */
public class MainTest {

/*
    public static void main(String[] args) {
        GroovyShell shell = new GroovyShell();
        String precondition = "def test1 = \"\"\n"
                              + "class Contest {\n"
                              + "    static String output = \"\";\n"
                              + "    static void printOutput(String text) {\n"
                              + "        output += text + \"\\n\"\n"
                              + "    }\n"
                              + "}";
        String script = "void process() {\n"
                        + "    for (def i = 1; i <= 10; i++) {\n"
                        + "        Contest.printOutput(i.toString())\n"
                        + "    }\n"
                        + "}\n"
                        + "process()\n";
        String output = "Contest.output";
        shell.evaluate(precondition);
        shell.evaluate(script);
        Object result = shell.evaluate(output);
        System.out.println(result);
    }
*/

}
